
bundle:
  name: uberprojectrohandab
  uuid: 21e80ae0-995e-4406-9ce5-7f0d5080805c

variables:
  catalog:
    description: The catalog to use


targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-d1fe5c51-9e6f.cloud.databricks.com
    variables:
      catalog: uber_cata
      source_schema: source
      bronze_schema: bronze
      silver_schema: silver
    root_path: /Workspace/Users/patelrohan160@gmail.com/.bundle/${bundle.name}/${bundle.target}
    source-linked: false
  # prod:
  #   mode: production
  #   workspace:
  #     host: http://127.0.0.1:7073
  #     # We explicitly deploy to /Workspace/Users/patelrohan160@gmail.com to make sure we only have a single copy.
  #     root_path: /Workspace/Users/patelrohan160@gmail.com/.bundle/${bundle.name}/${bundle.target}
  #   variables:
  #     catalog: uber_cata
  #     schema: prod
  #   permissions:
  #     - user_name: patelrohan160@gmail.com
  #       level: CAN_MANAGE
